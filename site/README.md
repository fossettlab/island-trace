# Story page: "The Island Trace"

`index.html` is a self-contained page (relative image paths in `img/`, Google Fonts for type, no scripts beyond a
range-input slider) telling how NARA 100384845 was identified and georeferenced. Copy the `site/` folder as-is to
publish it; nothing else in the repository is needed at runtime.
